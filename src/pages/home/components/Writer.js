import React, { Component } from "react";
import { WriterWrapper } from "../StyleComp";
class Writer extends Component {
  render() {
    return <WriterWrapper>Writer</WriterWrapper>;
  }
}

export default Writer;
